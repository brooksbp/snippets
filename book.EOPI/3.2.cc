// Design a deterministic scheme by which reads and writes to an
// uninitialized array can be made in O(1) time. You may use O(n)
// additional storage; reads to uninitialized entry should return 0.

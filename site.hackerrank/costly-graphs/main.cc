#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
  int T; cin>>T;
  while (T--) {
    int N; cin>>N;  // nodes
    int K; cin>>K;  // cost expoent: D^K
    
  }
  return 0;
}
